<%= div_for comment do %> 
	<p><strong>
		Posted <%= time_ago_in_words(comment.created_at) %> | 
		Author: <%= (User.find_by_id(comment.user_id).nil?)? "anonymous" : User.find_by_id(comment.user_id).name %> 
	</strong><br />
	<%= h(comment.body) %>
	</p>
<% end %>
	